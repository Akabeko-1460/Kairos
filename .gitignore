# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
out/
.next/

# Env files
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# Claude Code / Next.js Agents 向けの内部指示ファイル。プロジェクト固有の設計判断を含むため、
# GitHub上のリポジトリには置かず、ローカルにのみ保持する（ユーザー指示）。
# apps/web/AGENTS.md は next dev が自動生成する定型文なので、削除しても再度ローカルに現れうるが、
# ここでignoreしてあるため再track はされない。
CLAUDE.md
AGENTS.md
